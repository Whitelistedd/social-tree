import { Container, Name, SocialLoginButton, Title, Wrap } from './Login-styles'

import Image from 'next/image'

export const Login = () => {
  return (
    <Container>
      <Wrap>
        <Title>w</Title>
        <SocialLoginButton>
          <Name>Google</Name>
        </SocialLoginButton>
      </Wrap>
    </Container>
  )
}
